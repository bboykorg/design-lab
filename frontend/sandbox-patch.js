/* Design Lab — больше не используется.
 *
 * Попытка убрать allow-same-origin у окна предпросмотра не удалась: редактор
 * работает с DOM превью напрямую, и без общего origin перерисовка ломалась.
 * Правильное решение — обмен через postMessage или превью на отдельном домене.
 *
 * Файл оставлен пустым и только убирает следы прежнего опыта у тех, у кого
 * старая версия страницы осталась в кэше браузера.
 */
(function () {
  window.__dlSandboxPatched = true;
  try { localStorage.removeItem('dl_preview_same_origin'); } catch (e) {}
})();
