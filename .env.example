# Design Lab — backend proxy key (SAFE TO COMMIT: placeholder only).
# Copy to .env and paste your own key (comma-separated to rotate several).
# On Render set this as an environment variable instead of a file.
AI_API_KEY=
PUBLIC_URL=https://your-app.onrender.com
